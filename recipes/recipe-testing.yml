---
# yaml-language-server: $schema=https://schema.blue-build.org/recipe-v1.json
# image will be published to ghcr.io/<user>/<name>
name: bluewhale-testing
# description will be included in the image's metadata
description: Terra Mesa testing image. Personal OS image, not for distribution. Not the production bluewhale image.

# the base image to build on top of (FROM) and the version tag to use
base-image: ghcr.io/ublue-os/silverblue-main
image-version: 44 # latest is also supported if you want new updates ASAP

# module configuration, executed in order
# you can include multiple instances of the same module
modules:
  - type: files
    files:
      - source: system
        destination: / # copies files/system/* (* means everything inside it) into your image's root folder /

  - type: justfiles

  # Split build logic into smaller files to keep this readable.
  - from-file: packages/packages.yml
  - from-file: packages/copr.yml
  - from-file: packages/flatpaks.yml
  - from-file: packages/dms-hyprland-gnome.yml

  # Terra Mesa swap after shared package from-files so it replaces Mesa last.
  # Testing-only; production recipe.yml does not include this.
  - from-file: packages/terra-mesa.yml

  - type: brew
    direct-pull: true
    nofile-limits: true # increase nofile limits
    brew-analytics: true

  - type: systemd
    system:
      enabled:
        - rpm-ostreed-automatic.timer

  - type: kargs
    arch: x86_64
    kargs:
        - amdgpu.ppfeaturemask=0xffffffff # AMD overclocking

  - type: signing # this sets up the proper policy & signing files for signed images to work fully
