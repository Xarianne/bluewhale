# yaml-language-server: $schema=https://schema.blue-build.org/recipe-v1.json
# RPM Fusion + multimedia codecs.
# Implemented as a single script so the ffmpeg swap happens before any other
# RPM Fusion packages are installed, avoiding conflicts with ffmpeg-free.
modules:
- type: script
  scripts:
    - rpm-fusion-codecs.sh
