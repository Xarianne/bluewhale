---
# yaml-language-server: $schema=https://schema.blue-build.org/recipe-v1.json
# image will be published to ghcr.io/<user>/<name>
name: bluewhale
# description will be included in the image's metadata
description: This is my personal OS image. Not meant for distribution.

# the base image to build on top of (FROM) and the version tag to use
base-image: ghcr.io/ublue-os/kinoite-main
image-version: 44 # latest is also supported if you want new updates ASAP

# module configuration, executed in order
# you can include multiple instances of the same module
modules:
  - type: files
    files:
      - source: system
        destination: / # copies files/system/* (* means everything inside it) into your image's root folder /

  - type: justfiles

  # Split build logic into smaller files to keep this readable.
  # Use either negativo or terra-mesa if you are using terra-extras-multimedia
  - from-file: packages/packages.yml
  - from-file: packages/copr.yml
  - from-file: packages/third-party.yml
  - from-file: packages/flatpaks.yml

  - type: brew
    direct-pull: true
    nofile-limits: true # increase nofile limits
    brew-analytics: true

  - type: systemd
    system:
      enabled:
        - rpm-ostreed-automatic.timer
        - fish-defaults.service
        - brew-bundle.service
    user:
      masked:
        # input-remapper still works on demand via GUI or CLI; this only
        # prevents the autoload service from running at login
        - app-input\x2dremapper\x2dautoload@autostart.service

  - type: kargs
    arch: x86_64  # only inject kernel arguments to these specific OS architectures
    kargs:
        - amdgpu.ppfeaturemask=0xffffffff # AMD overclocking

  - type: os-release
    properties:
      NAME: Bluewhale
      PRETTY_NAME: Bluewhale

  - type: signing # this sets up the proper policy & signing files for signed images to work fully
